O que foi corrigido neste ZIP:
1. Program.cs:
   - removida variável connectionString duplicada;
   - removido UseMySql;
   - mantido somente UseNpgsql;
   - adicionada normalização para DATABASE_URL e DATABASE_PUBLIC_URL do Railway.

2. Migrations:
   - migrations antigas foram removidas para evitar referências MySQL/Pomelo.
   - gere novas migrations localmente para PostgreSQL.

3. SistemaBancarioSprint3.csproj:
   - removidos pacotes MySQL/Pomelo;
   - removidas referências duplicadas;
   - mantidos pacotes EF Core/Npgsql na versão 8.0.11.

Comandos recomendados após substituir os arquivos:

dotnet restore SistemaBancarioSprint3.csproj
dotnet build SistemaBancarioSprint3.csproj

dotnet ef migrations add InitialPostgres --project SistemaBancarioSprint3.csproj
dotnet ef database update --project SistemaBancarioSprint3.csproj

git add .
git commit -m "Fix PostgreSQL migration setup"
git push
