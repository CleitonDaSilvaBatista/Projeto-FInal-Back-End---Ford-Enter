Configuração feita:
- Program.cs corrigido para aceitar DATABASE_URL e DATABASE_PUBLIC_URL.
- Aceita formato postgresql://... e também Host=...;Username=...
- MySQL removido do Program.cs.
- Dockerfile criado/ajustado.
- .gitignore ajustado.

Próximos comandos:
git add .
git commit -m "Corrige PostgreSQL Railway"
git push

No Railway, no serviço do BACK-END, deixe a variável:
DATABASE_URL = valor da DATABASE_PUBLIC_URL do Postgres

Depois rode as tabelas:
dotnet ef database update --project SistemaBancarioSprint3.csproj
